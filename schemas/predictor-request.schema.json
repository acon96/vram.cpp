{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vram-cpp.local/schemas/predictor-request.schema.json",
  "title": "VRAM Predictor Request",
  "type": "object",
  "additionalProperties": false,
  "required": ["mode", "model", "runtime", "device"],
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["metadata", "fit"],
      "description": "Prediction mode: metadata-first estimator or full llama-fit projection."
    },
    "model": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source"],
      "properties": {
        "source": {
          "type": "string",
          "enum": ["local", "huggingface"]
        },
        "path": {
          "type": "string",
          "description": "Local file path or virtual path in wasm FS."
        },
        "huggingFace": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "repo": { "type": "string" },
            "file": { "type": "string" },
            "revision": { "type": "string" }
          }
        }
      }
    },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "required": ["n_ctx", "cache_type_k", "cache_type_v"],
      "properties": {
        "n_ctx": { "type": "integer", "minimum": 1 },
        "n_batch": { "type": "integer", "minimum": 1 },
        "n_ubatch": { "type": "integer", "minimum": 1 },
        "cache_type_k": { "type": "string" },
        "cache_type_v": { "type": "string" },
        "n_gpu_layers": { "type": "integer", "minimum": -1 }
      }
    },
    "device": {
      "type": "object",
      "additionalProperties": false,
      "required": ["host_ram_bytes"],
      "properties": {
        "host_ram_bytes": { "type": "integer", "minimum": 0 },
        "fit_target_mib": {
          "type": "array",
          "description": "Per-device llama-fit margin targets in MiB (maps to --fit-target).",
          "items": { "type": "integer", "minimum": 0 }
        },
        "target_free_mib": {
          "type": "array",
          "description": "Per-device desired free memory in MiB after fit adjustments.",
          "items": { "type": "integer", "minimum": 0 }
        },
        "gpus": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["id", "free_bytes"],
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "index": { "type": "integer", "minimum": 0 },
              "free_bytes": { "type": "integer", "minimum": 0 },
              "total_bytes": { "type": "integer", "minimum": 0 }
            }
          }
        }
      }
    },
    "fit": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fit_harness_binary": { "type": "string" },
        "min_ctx": { "type": "integer", "minimum": 0 },
        "execute_in_process": { "type": "boolean" },
        "show_fit_logs": { "type": "boolean" }
      }
    }
  }
}
