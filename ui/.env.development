# Local dev uses a dedicated assets server on a separate port.
# npm run assets:serve serves these URLs from build-wasm + web.
VITE_WASM_BASE_URL=http://127.0.0.1:8123/assets/
VITE_DEBUG_WASM_FIT_LOGS=1