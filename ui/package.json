{
  "name": "vram-cpp-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "assets:serve": "node ./scripts/serve-wasm-assets.mjs",
    "dev": "concurrently --kill-others \"npm run assets:serve\" \"npm run web\"",
    "web": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "concurrently": "^9.2.1",
    "svelte": "^5.55.4",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@huggingface/gguf": "^0.4.2"
  }
}
